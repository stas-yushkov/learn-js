// const playlist = {
//   tracks: [],
// };

// const propName = 'tracks';

// console.log(playlist[propName]);

const users = ['mango', 'poly'];

console.table(users);
console.log(users.length);

users.push('ajax');
console.table(users);
console.log(users.length);
